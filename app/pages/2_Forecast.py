import streamlit as st

st.title("Demand Forecast")
st.write("Forecast Model will be connected in upcoming parts.")
sku = st.text_input("Enter SKU")

if st.button("Predict"):
    st.success("Prediction module will be integrated.")